## 1. 🔍 어떻게 시도했는가? (How)

### ❌ [FAIL / SO-SO] 최초 접근 로직
* **🤔 생각한 흐름:**
    * 다음 라운드로 갈 때 각 숫자는 2와 `ceil` 나눗셈 연산한 결과값으로 갱신된다.
    * 각 숫자에 대한 `ceil`이 서로 일치할 때까지 반복
    * `ceil` 연산은 Math.ceil 없이 아래와 같은 방법으로 double precision 문제없이 연산 가능
```java
int x = (x - (2 - 1)) / 2;
```

---

## 2. 🎯 한 줄 본질 (What)
* 이 문제는 결국 `이진 트리 구조의 인덱스 패턴`을 파악해서 `반복문과 ceil 연산을 통해 구현`하는 문제다.

---
